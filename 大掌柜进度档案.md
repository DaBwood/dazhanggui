# 《大掌柜》开发进度档案

> 用途：跨对话同步进度。重开对话时，把本文件路径告知 Kimi（或直接说"读一下进度档案"）即可恢复上下文。
> 维护：每轮功能完成后更新「已完成清单」和「基准文件版本」。
> 2026-08-30 已瘦身：已稳定功能的实现细节以代码和 JSON 配置为准，档案只留结论与位置。

## 一、协作规则

1. 先确认需求再给代码，不确定就问，不猜；
2. 每个方法给注释，增删改的代码也注释（`【新增】`/`【改】`/`【修】`前缀）；
3. 不直接提交 GitHub，代码发对话栏，用户测试通过后自行提交；
4. 只给增删改代码片段，新增文件或架构重构才交付文件附件；
5. 对话过长需要重开时主动提醒；
6. **上传文件读取（2026-08-31 教训）**：粘贴长文会被平台转成怪文件名（含 # 空格 =），shell 转义不可靠，一律用 Python os.listdir 拿原始名再 open；文件 ls 可见但读不出=传输层损坏，请用户重传别反复试。可让用户试直传 .gd 或打 zip，通了就改流程（skill 目录只读写不进，此条以档案为准）。另：Windows 记事本存的 txt 可能带 BOM 或 GBK，读取用 `utf-8-sig` 兜底。
7. **与 Kimi 2.6 分工**（2.6 不占额度但跨文件逻辑不可靠）：
   - 2.6 做"局部装修"：单函数内的视觉微调（按钮尺寸/颜色/字号/坐标/文案），提示词钉死"只改指定函数、不许新增或重命名节点和字段、只输出改动行"，改完用户自验，对了不找 Kimi；
   - Kimi 做"结构工程"：跨文件接线、节点路径、JSON配置/存档迁移/数值公式、新功能——2.6 在这些地方翻过车（编字段名、路径拼错、功能倒退）；
   - 2.6 的产出跨出一个函数边界，默认先怀疑再验收；
   - 2.6 改过的文件测试通过后回传一次，Kimi 同步基准副本，防止两边版本漂移。

## 二、架构约定

- **代码仓库**：https://github.com/DaBwood/dazhanggui （2026-08-31 用户提供，以后找代码先上这里，可用 GitHub 工具直接读文件）

- **HeroData 门客领域库**：门客资质/赚速的**所有计算唯一入口**（hero_data.gd，class_name 静态类）。聚合方法首参 `g`（=GameData 中枢，不标类型避免循环引用）。总资质=初始+资质技能+晋升+珍兽+渔获技能+服装；基础赚速=总资质×等级×突破次数（⚠️已去除平方）；总赚速=基础×(1+百分比)+额外；全局贡献=总赚速×突破×0.5。改公式只改这一个文件。
- **hero.extra_income**：门客额外赚速池字段，原名 base_income（旧档自动迁移）。
- **GameData 中枢**：状态变量 + 转发器（带 return），对外 API 不变；新系统登记 = 声明变量/实例化 + 两个 systems 数组（存档合并、加载认领）。
- **systems 纯逻辑**：`var g` 引用中枢，成员访问加 `g.` 前缀；各系统自带 `get_save_data()/load_save_data()`。
- **pages 纯代码 UI**：`var c`（game_controller 根脚本）、`var data`（GameData）。
- **配置外置 JSON**：`res://data/*.json`，由 `_load_all_configs()` 加载，const 改 var 保留默认值兜底；概率用权重制（累积权重 roll，不要求和为 1）。
- **闯荡子视图**：入口挂 AdventureEntryGrid，子视图挂 AdventurePage 下，`switch_page` 要加 `hide_xxx_view()`。
- **弹窗**：`c._create_base_popup(标题, 尺寸)`（一律居中，pos 参数已忽略）+ `c._add_ok_button(vbox, 回调, 文本)`；底色 #2a2640 + 淡紫边框。
- **场景全代码驱动**：control.tscn 只剩根 Control+脚本；`_build_scene_shell()`（_ready 第一行）建全部壳层节点，`_apply_portrait_layout()`（_ready 末尾+size_changed）显式 position+size 布局，是布局唯一事实来源。静态文字在壳层代码顶部常量区。
- **立绘**：`res://assets/portraits/{friends|heroes|friends_cos|heroes_cos}/` 按ID命名（服装立绘 {id}_{cos_id}），零配置丢图即用，默认 .png。
- **全屏详情页**：`_bars_visible` 状态位 + `_set_bars_visible()`，顶栏底栏显隐随状态，PageContainer 矩形二选一。

## 三、已完成清单（按模块）

- **基础框架**：L0-L3 重构；横屏转竖屏，基准分辨率 **600×1067**（9:16）；场景收编全代码驱动（壳层代码.gd）。
- **门客赚速体系统一**：HeroData 封装全部计算；base_income→extra_income 改名（含存档迁移+heroes.json）；基础赚速 突破²→突破；删旧版双轨增长。
- **庄园玩法**：农场/牧场/宅院三页签。宅院 32技艺×2卷（无上限无门槛，消耗公式 a+b×等级^p 配在 courtyard.json，消耗庄园仓库产物）；108门客分32组、116挚友分12组写死在 courtyard.json；产物统一鼬毛/狐尾/鹿茸/蜂蜜（熊改蜜蜂带旧档迁移）。宅院/农场/牧场均为按钮+弹窗升级（弹窗内十连勾选）。
- **商战玩法**：小队上限=门客总数÷6+1。
- **挚友目标**（按钮弹窗式）。
- **体力丹**（恢复1点游历体力可超上限）+体力礼包+4新礼物。
- **一键游历**：勾选后按当前体力快照连开，杜康只加不扣，结果汇总弹窗。
- **一键贸易**：勾选后每秒 auto_trade_tick，Boss就绪只谈判不贸易，战力够自动谈判进章，不足自动停；开关不进存档。
- **三页排序**：挚友按友好度、门客按实时赚速、珍兽装备优先按装备门客赚速；人参/珍兽装备/商战组队 3 处选择器同规则。
- **门客缘分按钮**：HeroPanel 弹窗反查 bound_heroes 挚友列表。
- **关卡宝箱批量开启**：stage_box.json 权重制掉落，结果弹窗（被产物卡复用）。
- **产物卡**：按当前庄园产量发12小时全部产物入庄园仓库。
- **盒子礼包**：门客/挚友/物品盒（SPECIAL_PACKS，各1元）+ 蛮荒礼盒（10000元，自选玉灵芝/虎魄耀石/龙魂精元/扶桑枝×100），背包弹选择器使用。
- **蛮荒主兽单独兑换**：乘黄/陆吾/应龙/金乌各吃专属道具（beasts.json 改 exchange_item，仍100兑1），相柳保留九渊之水；4种新道具获取途径待活动。
- **垂钓系统**：瀑湖+天池双钓点（切换在钓点弹窗内，界面状态 _loc 不入存档；天池消耗赤龙，赤龙不自动恢复、仅礼包/活动获取）；瀑湖新增6条无双（绵阳海兔/邓氏鱼/奇虾/蝠鲼/白鲸/天使，unlock_fish+unlock_tier 机制：对应无双鱼养成≥20阶才入池）；天池专属新品级**极.无双**6条（蓝粉海星/圣诞精灵/海蝴蝶/冥河水母/竖琴海豹/湾鳄，权重1，养成=无双档但晋升6%/阶、技能+6资质/级、技能经验1800/级，技能名"神.XXX"前缀，图鉴奖励=无双翻倍）；**保底兑换**（钓点弹窗底部：按累计消耗计进度可累计多份，瀑湖500地龙兑1无双/天池200赤龙兑1极.无双，自选列表含未解锁条件鱼，进度满显示红点，存档字段 fishing_dilong_spent/fishing_chilong_spent，旧档不追溯，兑换不算"垂钓"不计任务）；时段+天气影响鱼池；地龙消耗/小时恢复；撒网十连；任务13种上限3个+图鉴首获奖励+红点；渔获存 fishing_storage 不进背包，奖励发背包。加鱼只改 fishing.json 的 pools/extra_pools。
- **门客渔获装备**（模型A·按种类养成）：无双/传奇/普通三档晋升+技能喂养，养成数据 fishing_fish_dev，装备标记 heroes[].equipped_fish；HeroData 三处接入；配置在 fishing.json 的 fish_equip 段。
- **服装系统**：costumes.json + costume_system.gd + costume_view.gd；门客63人214件+挚友90人（含新建挚友"幻戏"huan_xi）；兑换→手动解锁→升级三段（库存制）；8系列+系列光环（玉璜升级）；hero_page 服装/光环标签页；兑换视图加服装层；8种系列兑换道具（获取途径未接）。
- **门客面板 v4**：全屏代码构建（_ensure_hero_panel_code，z_index=10）；技能/副业/服装/光环四标签；赋诗/晋升 PromoBtn 固定位；升级按钮内显消耗（公式 1.05^lv，显示扣费已统一）。
- **挚友详情页重构**：立绘背景铺满+顶/底半透明底条；技能/美名/游玩/服装四标签点击直接开弹窗；底部礼物栏6键+十连勾选；详情页全屏化（顶底栏隐藏，「<」返回）。
- **府邸页/背包页按钮化**：府邸入口整卡可点（120×40 四列）；背包道具单 Button（名称+数量两行、4列），点击开详情弹窗使用；hide_in_bag 标记跳过。
- **资质丹固定消耗**：吃资质丹的技能，消耗=每级加的资质数（门客资质技能 普通1~完美5、服装技能 素2华2锦3）；店铺技能（吃算盘）保持 1.05 曲线不动。
- **兽魂系统**（2026-08-30）：珍兽培养面板【兽魂】按钮→魂盘弹窗。每只珍兽实例一个5×5魂盘（初始解锁正中3×3），额格外解锁扣五色石=魂盘等级×5（重置全退）；等级阶梯 10格2级/13格3级/18格4级/25格5级，容量=3+等级-1块。魂石4品质（优秀1格/卓越2格/传奇3格/无双4格，形状库随机不旋转不重叠限已解锁格），每格随机职业色+词条资质，格色==装备门客职业才激发（赚速+按等级3/5/10/15/20% + 词条资质）。**满盘光环**加成（已解锁格填满按魂盘Lv.+5/10/15/20/30%赚速，full_pct 配置，光环按钮满亮灰灭点按弹详情）。宝箱：魂石宝箱=随机优秀、无双魂石箱=四格同色无双（背包使用进仓库）；重塑=同品质2块概率升品（40%/20%/10%，失败同品全重随），无双不可重塑。数据：魂石仓库 g.soul_stones（中枢存档），魂盘存珍兽实例 instance["soul"]；加成经 HeroData 两行接入（get_hero_soul_percent/aptitude）。文件：soulstones.json + soul_system.gd + soul_view.gd（兽魂玩法/）。
  - v2→v3（同日）：兽魂改**独立全屏页** SoulPage（z_index=35，子弹窗40；盖珍兽详情z30弹窗/全屏化后z20），拖拽放置（内置类 SoulCell 拖放目标 + StoneCard 拖放源，点按=词条弹窗）；**DragGhost 手动幻影**（引擎 set_drag_preview 在部分环境不渲染；幻影与魂盘格子同大96同距8，每帧钉指尖，拖完自毁）；**落点不做绿框预判——松手按幻影左上角相对魂盘的位置整体换算形状落点**（_ghost_anchor，四舍五入对齐盘格，交 can_place_stone 校验，按形状不按锚点）；形状小图不再画锚点金框。层级新约定：**全屏页 z20（珍兽详情）/ z35（兽魂页），页内弹窗 z30→40，飘字 z50，拖动幻影 z60**。
  - v3→v4（同日）：修"单格魂石永远放不上、多格完全对正反而放不上"——根因有二：①`_make_shape_mini` 的色块 blk（Panel）mouse_filter 默认 STOP，z60 幻影的色块截获拖放命中（单格幻影=一整块色块永远挡住）→ panel/blk 全改 IGNORE；②多格完全对正时指尖正好落在幻影内缝=魂盘 8px 格缝，格缝属 GridContainer 不属任何 SoulCell → 新增内置类 **SoulBoardGrid extends GridContainer**（STOP + _can_drop_data/_drop_data 委托同一套 _board_can_drop/_board_drop），格缝松手也能落。**教训：拖放路径上所有纯装饰节点必须显式 IGNORE；容器缝隙要自己当拖放目标兜底**。
  - v4→v5（同日）：**魂石仓库改方形按钮网格**——一格一块只显示形状（StoneCard 88×88，GridContainer 6列居中，形状小图 px=20 居中，2px 品质色边框认品质），点按=词条弹窗/拖动=上盘不变。
  - v5→v6（2026-08-31）：**魂盘光环（满盘加成）**——已解锁格被魂石全部填满（不要求颜色激发）时，装备门客赚速额外 +5/10/15/20/30%（按魂盘Lv.1~5，配置 soulstones.json settings.full_pct），直接并入 get_board_bonus 的 percent（HeroData 零改动）；新增 soul_system.get_fill_progress()/is_board_full()；兽魂页操作行加【魂盘光环】按钮（满=金亮/不满=灰，点击弹详情：状态+进度+各等级加成表，弹窗名 SoulAuraPopup 已加入关闭清单）。⚠️基准 soul_view.gd 补录 STONE_COLS=6/STONE_BTN=88 常量（v5 片段里的，此前基准漏记）。**用户测试通过，魂石系统至此完整收官。**
  - ~~已知待办：手机触屏拖不动~~ **已解决**（2026-08-31 用户手机实测通过：v4 修完命中测试+格缝兜底后，手机拖拽放置正常）。兽魂系统编辑器+手机双端验收通过，**魂骨系统**（预告的下一个珍兽玩法，照全屏页套路加入口）随时可开工。
  - 珍兽详情全屏化（beast_page.gd，2026-08-30 已交付）：BeastDetailPanel 弹窗→根节点全屏页（z20，Panel+显式size，顶栏"< 返回"），节点名/child(0)=VBox 不变所以 _update_beast_detail 等不动；**不再碰 _current_popup/Overlay**（_input 里"点外面关 _current_popup"那套就是弹窗套娃关不掉的根因）；技能刷新板仍挂它下面居中。基准存 第3批重构/beast_page.gd。后续魂骨系统同样做全屏页入口。
- **魂力培养（魂骨/魂体）**（2026-08-31 已交付待测试）：珍兽详情页【魂力】按钮（兽魂旁）→ HunliPage 全屏页（z35/弹窗z40，确认弹窗z45）。每只珍兽实例一个魂体（6槽：头/左臂/右臂/躯干/左腿/右腿，初始解锁1，免费解锁不可回锁、解锁不分顺序），存 instance["hunli"]；魂骨仓库 g.soul_bones（中枢存档）。魂骨5品级（百年/千年/万年/十万年/百万年）：资质=阶×5/10/30/80/200（上限20阶，每阶吃对应晶核13/5/4/2/1），百万年额外赚钱每阶+10%；技能按品级固定（部位只是槽位区分）：赚钱技能加固定赚速（4500×星数/级）、资质技能加资质（星数/级），上限50级，升级吃淬骨精尘（赚钱技能星×3、资质技能星×10/级）；百万年幻影突袭=五星（表格原写三星是笔误，已改）；⚠️技能初始0级需手动升（要改初始1级改 gen_bone 一行）。魂体升级60龙芝草/级+6资质、上限200。共鸣=已装骨数1~6→基础资质50~100 ×印记倍率（百年1/千年4/万年5/十万年6/百万年7，≥4枚起每多1枚+0.1，不足不惩罚）。回收=仓库骨返对应晶核×1+精尘120/540/1760/4220/8640（投入不返还）。魂骨盒子（hun_gu_box）背包使用自选部位+品级（测试用）。加成经 HeroData 三处接入（资质/固定赚速/百分比，同兽魂接法）。文件：soulpower.json + soulpower_system.gd + soulpower_view.gd（魂力培养/）。新道具全走 items 轨：long_zhi_cao/cui_gu_jing_chen/jinghe_bainian/jinghe_qiannian/jinghe_wannian/jinghe_shiwannian/jinghe_baiwannian/hun_gu_box。
- **自建弹窗居中+背景修复**（2026-08-31）：商铺页 `_show_hero_assign_selector`（门客派遣选择器）和背包页 `_show_ginseng_selector`（人参选择器）均为自建 Panel 未走 `_create_base_popup` 工厂，position 硬编码 `Vector2(326,150)` 在 600 宽基准分辨率下偏右出界；修复：改按 `get_viewport_rect().size` 动态居中，补 `StyleBoxFlat` 背景色 `#1e1b2e` 解决与底层混淆。档案「收尾待办」第 3 条命中。连带 bag_page 人参选择器同修。

## 四、基准文件版本（/mnt/agents/output/ 下）

- 第2批重构/：game_data.gd（含庄园/商战/挚友目标/宅院/服装）+ hero_data.gd + systems
- 第3批重构/：game_controller.gd（**2026-08-31 已回传同步为最新 1843 行版**——含存档三保险/退出按钮、_bars_visible 二级页全屏化、_recenter_popup 弹窗二次居中、soul_view 注册，此前"不含回传后片段"的漂移已消除）+ pages（bag_page/hero_page/friend_page 已于 2026-08-31 回传同步为最新——含背包按钮化、门客面板v4、挚友详情全屏化、2.6手机滑动修复：滚动列表内按钮 mouse_filter=PASS 让触摸滑动穿透到 ScrollContainer）
- 庄园玩法/、商战玩法/、挚友目标/、关卡宝箱/、宅院玩法/、垂钓玩法/（fishing_system / fishing_view / fish_equip_view / fishing.json）。fishing.json 基准=天池+保底版最新；三个 .gd 已于 2026-08-30 用户回传同步（含保底兑换、误扣地龙修复、整数除法警告压制、兑换按钮居中）
- 场景收编/壳层代码.gd（含全部修复）
- 配置：friends.json、heroes.json、items.json、travel.json、stage_box.json、courtyard.json、costumes.json
- scene_tree.txt：场景树快照（纯参考）
- 手机测试/serve_web.py（HTTPS 自签名零依赖版）
- 兽魂玩法/：soulstones.json + soul_system.gd + soul_view.gd（兽魂系统，.txt 为交付副本）
- 魂力培养/：soulpower.json + soulpower_system.gd + soulpower_view.gd（魂力培养系统，.txt 为交付副本；接入片段见 2026-08-31 对话，用户本地应用后基准待回传）
- ⚠️ mansion_page.gd / shop_page.gd（含派遣选择器居中+背景修复）按钮化片段、beast_page.gd / game_data.gd / items.json 光环与货币迁移片段用户已本地应用，基准待下次回传同步。（bag_page 已于 2026-08-31 同步，从此清单移除）
- ~~⚠️ hero_page.gd 资质丹 `<=` 应为 `<`~~（2026-08-31 用户已应用，基准已同步修复）

## 四点六、珍兽光环养成（2026-08-30）

- 光环拆三行独立显示：光环一（系列光环）=10%+(特殊系列兽数-1)×10% 自动不可升级；光环二初始1级+5%/级、集齐同系列解锁升级、满级20、消耗该兽兑换道具 当前等级×10；光环三每级+1珍兽等级上限（上限=200+光环三等级，原写死200已改）、光环二满级解锁升级；特例：星神圣兽光环二=全珍兽技能加成每只贡献 100%+10%×(lv-1) 多只【累加】、无光环三整行不显示；**光环三全局生效：全员上限=200+所有特殊兽光环三等级之和**（升级仍按单只兽操作）。等级存珍兽实例 aura2_lv/aura3_lv（旧档补1）。
- ⚠️**双轨货币坑（已修）**：珍兽果/奇香果曾有独立货币（产出/消耗全走它）和背包道具版（空壳）两套并存——纯历史遗留（商城礼包时代写变量，后续玩法全抄作业）。**修复方向：废除独立货币、并入 items 道具轨**（game_data 删2个 var；mall/item/travel/fishing 四处产出改 items；beast_system 消耗与存档迁移改 items，旧档顶层字段一次性并入；beast_page 显示改读道具轨；hide_in_bag 方案已作废不加）。教训：**专用耗材一律做成普通道具进 items，不要再开独立货币 var**。

## 四点七、存档三保险 + 退出按钮（2026-08-31，已测试通过）

- **问题**：手机 Web 端"只进旧档、怎么玩都不保存"——旧版 save_game 只在启动/商城购买/退出时触发，而手机划掉标签页浏览器不给 tree_exiting 机会，退出存档永远跑不到；每次启动那句"先保存防止闪退"反而把旧状态又存了一遍，存档永远停在上次打开时的快照。
- **修复**（全在 game_controller.gd）：①定期自动存档：on_auto_earn 每秒计数，满 AUTOSAVE_INTERVAL（30秒）save_game；②Web 切后台补存：_setup_web_save_hook 用 JavaScriptBridge 监听 pagehide+visibilitychange，页面隐藏瞬间写盘（回调引用存 _web_hide_cb 防释放）；③顶栏右侧「退出」按钮→二次确认弹窗→存档后退出：非 Web get_tree().quit()，Web 尝试 window.close()（多数浏览器拦截脚本关窗）+ 飘字"已存档，可安全关闭页面"。
- **教训**：Web/手游端存档必须"定期+页面隐藏钩子"，不能依赖退出通知；浏览器返回手势不经过游戏代码，"按返回退出"做不了，用显式退出按钮。

## 五、踩坑教训速查（详表见 skill references/godot4-pitfalls.md）

1. 新建 Button 当帧 size=(0,0)，显式赋 size 或用常量定位。
2. 代码面板必须显式 z_index（10=面板，30=弹窗）；**弹窗一律挂 c 根节点**，z_index 只对兄弟节点有硬保证。
3. 这批节点**禁用锚点预设**（FULL_RECT 会把 0 尺寸焊成负偏移），一律 PRESET_TOP_LEFT 归零 + 显式 position+size。
4. GDScript 子块变量不能遮蔽父块后声明的同名变量；整数除法警告用 `/ 10.0`。
5. 节点挪容器后，所有 get_node 路径字符串必须同步加层。
6. custom_minimum_size 只是下限，长文本照样撑大 Button/格子——clip_text 或拆行；子节点 min_size 还会把父容器 size 赋值钳回去。
7. PanelContainer 实际尺寸=max(custom_minimum_size, 内容最小尺寸)，工厂钳制管不住内容撑大，弹窗居中靠 `_recenter_popup` 等两帧二次居中。
8. 沙盒里 write_file 创建的文件 Python open() 覆盖会 PermissionError——先 shell rm 再写。
9. **Godot 代码规范：缩进用 Tab（\t），不是 4 个空格。** 用户提醒：GDScript 官方规范是 tab 缩进，我输出时误用 4 空格会导致用户粘贴后格式错乱。
10. **手机端滚动列表里的整卡 Button 默认 mouse_filter=STOP 会拦截触摸滚动**（列表几乎划不动）——按钮改 `MOUSE_FILTER_PASS`，点击照常生效、滑动穿透到 ScrollContainer。2.6 已在背包/门客/挚友列表应用。
11. **Web 端手机虚拟键盘输入无效是引擎已知 bug（godot#99602）**：LineEdit/SpinBox 靠隐藏 HTML input 同步，Chromium 系手机浏览器+多数输入法的组合事件引擎收不到（键盘能唤起、打字没反应；Firefox 手机版正常）。**绕法：数量输入改按钮步进（+100/+1000 等），别依赖键盘**——物品盒子弹窗已改（bag_page `_show_item_box_selector`：描述改纯名字防横滚、列表按钮补 PASS、加 +100/+1000 按钮）。

## 六、待开发线索

- ~~钓鱼第二钓点~~（天池已完成）；第二钓具（改 fishing.json 的 gears 即可）。~~鱼食用途~~（已做成渔获技能喂养材料，1经验/个）。
- 侠义/农业/仕途/工业/经商之道的用途（desc 写"后续玩法使用"）。
- 病人手册（医馆玩法？）、促织笼（促织玩法？）。
- 服装 8 种系列兑换道具的获取途径（接活动）。
- 门客光环标签页（占位）、挚友立绘长期需横屏构图。
- 顶栏"赚速"文字截断（600宽降档后未再反馈，用户提起再处理）。

## 七、收尾待办（稳定后清一遍）

1. 删诊断代码：game_controller `_debug_page_rects()`、bag_page 诊断 print（若还在）。
2. 壳层静态文字核对：NAV_LABELS 前两个"府邸/商铺"为推断值、充值页签文案。
3. 弹窗残余核对：若还有歪的，多半是没走 `_create_base_popup` 的自建 Panel。
4. **正式发布前**（远期）：字体换 MiSans/思源黑体等免费可商用字体（现微软雅黑有授权问题）；导出换 release 模板；评估 PWA 全屏。

## 八、手机真机测试

链路已全通（详见 skill references/godot4-pitfalls.md 末节）：Web 单线程导出覆盖目录 → `python serve_web.py "D:\godot_web_test"` → 电脑 Edge https://localhost:8000 / 手机夸克浏览器 https://192.168.14.15:8000（手动带 https://，证书警告点继续）。复测=重新导出→手机刷新，服务器不用重启。

## 九、重开对话开场模板

```
我在开发 Godot 4.7 放置经营手游《大掌柜》。先读 /mnt/agents/output/大掌柜进度档案.md 恢复上下文。
本次要做：<新需求>
```
